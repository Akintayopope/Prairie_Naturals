test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

render_disk:
  service: Disk
  # On Render, mount a Disk at /var/data (or set ACTIVE_STORAGE_ROOT to your mount)
  root: <%= ENV.fetch("ACTIVE_STORAGE_ROOT", "/var/data/active_storage") %>

cloudinary:
  service: Cloudinary
