<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Prairie Naturals" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- HEAD CLEANUP (keep only one) -->
<%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
<%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
<!-- Modern font -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">


  </head>

  <body class="pn-body">
<header class="pn-header" role="banner">
  <!-- Promo bar -->
  <div class="pn-promo">
    <div class="container d-flex align-items-center justify-content-between">
      <div class="pn-promo__center mx-auto text-center">
        <strong class="me-2">15% sitewide!</strong>
        use promo code <span class="pn-promo__code">LOVE15</span>
      </div>

      <div class="pn-promo__social d-none d-md-flex gap-2">
        <a href="#" aria-label="Instagram"><i class="bi bi-instagram"></i></a>
        <a href="#" aria-label="Facebook"><i class="bi bi-facebook"></i></a>
        <a href="#" aria-label="YouTube"><i class="bi bi-youtube"></i></a>
        <a href="#" aria-label="X"><i class="bi bi-twitter-x"></i></a>
      </div>
    </div>
  </div>

  <!-- Main navbar (note: no sticky-top here) -->
  <nav class="navbar navbar-expand-lg pn-navbar">
  <div class="container-fluid px-4">
    <!-- Brand -->
    <%= link_to root_path, class: "navbar-brand fw-bold d-flex align-items-center gap-2" do %>
      <span class="brand-text">Prairie Naturals</span>
    <% end %>

    <!-- Mobile toggle -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"
            aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div id="mainNav" class="collapse navbar-collapse">
      <!-- Use CSS Grid on lg+ to spread content -->
      <div class="w-100 d-lg-grid align-items-center gap-3"
           style="grid-template-columns: 1fr 1.4fr auto;">

        <!-- Primary links -->
        <ul class="navbar-nav order-2 order-lg-1">
          <li class="nav-item"><%= link_to "Shop", storefront_products_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "About", about_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Contact", contact_path, class: "nav-link" %></li>
        </ul>

        <!-- Search (grows in the middle) -->
        <div class="order-3 order-lg-2 ms-lg-n3">
          <%= form_with url: storefront_products_path, method: :get, local: true, class: "pn-search w-100" do %>
            <i class="bi bi-search" aria-hidden="true"></i>
            <input id="q" type="search" name="q" value="<%= h(params[:q] || params[:search]) %>"
                   class="form-control pn-search-input w-100"
                   placeholder="Search products…">
          <% end %>
        </div>

        <!-- Right actions -->
        <div class="order-1 order-lg-3 d-flex align-items-center justify-content-lg-end gap-2">
          <% if user_signed_in? %>
            <%= link_to orders_path, class: "btn btn-link px-2 pn-link" do %>
              <i class="bi bi-person-check"></i> <span class="d-none d-md-inline">My Orders</span>
            <% end %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary btn-sm btn-lift" %>
          <% elsif admin_user_signed_in? %>
            <%= link_to send("#{ActiveAdmin.application.default_namespace}_root_path"),
                        class: "btn btn-link px-2 pn-link" do %>
              <i class="bi bi-speedometer2"></i> <span class="d-none d-md-inline">Admin</span>
            <% end %>
            <%= button_to "Logout", destroy_admin_user_session_path, method: :delete, class: "btn btn-outline-secondary btn-sm btn-lift" %>
          <% else %>
         <%= link_to new_user_session_path, class: "btn btn-outline-light-theme btn-sm btn-lift" do %>
        <i class="bi bi-box-arrow-in-right"></i> Log in
          <% end %>


            <%= link_to new_user_registration_path, class: "btn btn-primary btn-sm btn-lift" do %>
              <i class="bi bi-person-plus"></i> Sign up
            <% end %>
          <% end %>

          <%= link_to cart_path, class: "btn btn-dark btn-lift d-flex align-items-center gap-2" do %>
            <i class="bi bi-bag"></i>
            <span class="d-none d-sm-inline">Cart</span>
            <span class="badge text-bg-light"><%= cart_count %></span>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
</header>

</nav>
</header>

<% if @is_homepage %>
  <section class="pn-hero">
    <div class="container-xxl">
      <div class="pn-hero__content text-center mx-auto">
        <h1 class="pn-hero__title">Pure Nature, Pure Wellness</h1>
        <p class="pn-hero__subtitle">
          Discover our carefully curated collection…
        </p>
        <%= link_to "Explore Products", storefront_products_path, class: "btn btn-cta" %>
      </div>
    </div>
  </section>
<% end %>




<main class="container-xxl my-4">
<%= render "shared/breadcrumbs" %>
<%= render "shared/flash" %>
  <%= yield %>
</main>


<!-- Floating cart (unchanged) -->
<div id="floating-cart" class="pn-floating-cart" aria-live="polite">
  🛒 <%= link_to 'Cart', cart_path, class: "text-white text-decoration-none" %>
  (<%= cart_count %> items — <%= number_to_currency(cart_total) %>)
</div>

<!-- FOOTER -->
<footer class="pn-footer" role="contentinfo">
  <div class="container-xxl">
    <div class="row gy-4">

      <!-- Contact / Store -->
      <section class="col-12 col-md-4" aria-labelledby="footer-contact">
        <h6 id="footer-contact" class="pn-footer__heading">Contact Us</h6>
        <address class="pn-footer__text mb-3 not-italic">
          742 Prairie View Drive<br>
          Winnipeg, MB R3Z 5K2
        </address>
        <p class="pn-footer__text mb-2">
          Mon–Fri: 11am–10pm<br>
          Sat–Sun: 11am–12pm
        </p>
        <p class="pn-footer__text mb-0">
          Tel: 123-456-7890<br>
          Email: info@prairienaturals.com
        </p>
      </section>

      <!-- Quick Links -->
      <nav class="col-6 col-md-4" aria-labelledby="footer-quick-links">
        <h6 id="footer-quick-links" class="pn-footer__heading">Quick Links</h6>
        <ul class="list-unstyled pn-footer__list">
          <li><%= link_to "All Products", storefront_products_path, class: "pn-footer__link" %></li>
          <li><%= link_to "New", storefront_products_path(sort: "newest"), class: "pn-footer__link" %></li>
          <li><%= link_to "Best Sellers", storefront_products_path(sort: "bestsellers"), class: "pn-footer__link" %></li>
          <li><%= link_to "Shipping & Returns", storefront_shipping_returns_path, class: "pn-footer__link" %></li>
          <li><%= link_to "Store Policy", storefront_store_policy_path, class: "pn-footer__link" %></li>
          <li><%= link_to "Payment Methods", storefront_payment_methods_path, class: "pn-footer__link" %></li>
          <li><%= link_to "FAQ", storefront_faq_path, class: "pn-footer__link" %></li>
        </ul>
      </nav>

      <!-- Newsletter / Note (kept your sentence, removed social icons) -->
      <section class="col-6 col-md-4" aria-labelledby="footer-news">
        <h6 id="footer-news" class="pn-footer__heading">Newsletter</h6>
        <p class="pn-footer__text mb-0">Join our newsletter for natural tips and offers.</p>
      </section>

    </div>

    <hr class="pn-footer__rule my-4">

    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center gap-3">
      <p class="pn-footer__muted mb-0">
        © <%= Time.current.year %> Prairie Naturals. All rights reserved.
      </p>
      <div class="d-flex gap-3 small">
        <%= link_to "Privacy Policy", storefront_privacy_policy_path, class: "pn-footer__link" %>
        <%= link_to "Terms of Service", storefront_terms_path, class: "pn-footer__link" %>
      </div>
    </div>
  </div>
</footer>

  </body>
</html>
