<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Prairie Naturals" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- HEAD CLEANUP (keep only one) -->
<%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
<%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
<!-- Modern font -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">





  </head>

  <body class="pn-body">
  <!-- Unified Header -->
<!-- Top Promo & Social Bar -->
<div class="top-bar bg-black text-white py-1 small px-3 ">
  <div class="d-flex align-items-center justify-content-between w-100">


    <!-- Center: promo -->
    <div class="text-center flex-grow-1">
      <span class="fw-bold">15% sitewide!</span>
      use promo code <span class="text-uppercase">LOVE15</span>
    </div>

    <!-- Right: social -->
    <div class="d-flex gap-3">
      <a href="#" class="text-white"><i class="bi bi-instagram"></i></a>
      <a href="#" class="text-white"><i class="bi bi-facebook"></i></a>
      <a href="#" class="text-white"><i class="bi bi-youtube"></i></a>
      <a href="#" class="text-white"><i class="bi bi-twitter-x"></i></a>
    </div>

  </div>
</div>


  <!-- Your existing navbar starts here -->
  <!-- NAVBAR: replace your existing <nav>...</nav> with this -->
<nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top pn-nav">
  <div class="container">
    <!-- Brand -->
    <%= link_to root_path, class: "navbar-brand fw-bold d-flex align-items-center gap-2" do %>
      <%= image_tag "logo.png", alt: "Prairie Naturals", height: 28, class: "rounded-2 shadow-sm" rescue nil %>
      <span class="brand-text">Prairie Naturals</span>
    <% end %>

    <!-- Mobile toggle -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"
            aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div id="mainNav" class="collapse navbar-collapse">
      <!-- Left: primary links -->
      <ul class="navbar-nav me-3">
        <li class="nav-item"><%= link_to "Shop", storefront_products_path, class: "nav-link" %></li>
        <li class="nav-item"><%= link_to "About", about_path, class: "nav-link" %></li>
        <li class="nav-item"><%= link_to "Contact", contact_path, class: "nav-link" %></li>
      </ul>

      <!-- Center: search (grows) -->
      <div class="flex-grow-1 px-lg-2 my-2 my-lg-0">
        <%= form_with url: storefront_products_path, method: :get, local: true, class: "pn-search" do %>
          <i class="bi bi-search"></i>
          <input type="search" name="q" value="<%= h(params[:q] || params[:search]) %>" class="form-control pn-search-input"
                 placeholder="Search products…">
        <% end %>
      </div>

      <!-- Right: account + cart -->
      <div class="d-flex align-items-center gap-2 ms-lg-2">
        <% if user_signed_in? %>
          <%= link_to orders_path, class: "btn btn-link px-2 pn-link" do %>
            <i class="bi bi-person-check"></i> <span class="d-none d-md-inline">My Orders</span>
          <% end %>
          <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary btn-sm btn-lift" %>
        <% elsif admin_user_signed_in? %>
  <%= link_to send("#{ActiveAdmin.application.default_namespace}_root_path"),
              class: "btn btn-link px-2 pn-link" do %>
    <i class="bi bi-speedometer2"></i> <span class="d-none d-md-inline">Admin</span>
  <% end %>
  <%= button_to "Logout", destroy_admin_user_session_path, method: :delete, class: "btn btn-outline-secondary btn-sm btn-lift" %>

        <% else %>
          <%= link_to new_user_session_path, class: "btn btn-outline-secondary btn-sm btn-lift" do %>
            <i class="bi bi-box-arrow-in-right"></i> Log in
          <% end %>
          <%= link_to new_user_registration_path, class: "btn btn-primary btn-sm btn-lift" do %>
            <i class="bi bi-person-plus"></i> Sign up
          <% end %>
        <% end %>

        <%= link_to cart_path, class: "btn btn-dark btn-lift ms-1 d-flex align-items-center gap-2" do %>
          <i class="bi bi-bag"></i>
          <span class="d-none d-sm-inline">Cart</span>
          <span class="badge text-bg-light"><%= cart_count %></span>
        <% end %>
      </div>
    </div>
  </div>
</nav>





    <!-- Flash -->
    <div class="container mt-0">
      <% if notice %><div class="alert alert-success shadow-sm"><%= notice %></div><% end %>
      <% if alert  %><div class="alert alert-danger  shadow-sm"><%= alert  %></div><% end %>
    </div>

    <!-- Page content -->
    <%= yield %>

    <!-- Floating cart -->
<div id="floating-cart" class="pn-floating-cart" aria-live="polite">
  🛒 <%= link_to 'Cart', cart_path, class: "text-white text-decoration-none" %>
  (<%= cart_count %> items — <%= number_to_currency(cart_total) %>)
</div>

<!-- Footer -->
<footer class="bg-dark text-white pt-5 pb-4 mb-0">
  <div class="container">
    <div class="row gy-4">

      <!-- Shop -->
      <div class="col-6 col-md-3">
        <h6 class="text-uppercase fw-bold mb-3">Shop</h6>
        <ul class="list-unstyled mb-0">
          <li><%= link_to "All Products", storefront_products_path,                   class: "text-white-50 text-decoration-none d-block py-1" %></li>
          <li><%= link_to "New",          storefront_products_path(sort: "newest"),   class: "text-white-50 text-decoration-none d-block py-1" %></li>
          <li><%= link_to "Best Sellers", storefront_products_path(sort: "bestsellers"), class: "text-white-50 text-decoration-none d-block py-1" %></li>
          <li><%= link_to "Nature",       storefront_products_path(category: "Nature"), class: "text-white-50 text-decoration-none d-block py-1" %></li>
        </ul>
      </div>

      <!-- Our Store -->
      <div class="col-6 col-md-3">
        <h6 class="text-uppercase fw-bold mb-3">Our Store</h6>
        <address class="text-white-50 mb-3">
          742 Prairie View Drive<br>
          Winnipeg, MB R3Z 5K2
        </address>
        <div class="text-white-50 small mb-3">
          Mon–Fri: 11am–10pm<br>
          Sat–Sun: 11am–12pm
        </div>
        <div class="text-white-50 small">
          Tel: 123-456-7890<br>
          Email: info@prairienaturals.com
        </div>
      </div>

      <!-- Policy -->
      <div class="col-6 col-md-3">
        <h6 class="text-uppercase fw-bold mb-3">Policy</h6>
        <ul class="list-unstyled mb-0">
          <li><%= link_to "Shipping & Returns", storefront_shipping_returns_path, class: "text-white-50 text-decoration-none d-block py-1" %></li>
          <li><%= link_to "Store Policy",        storefront_store_policy_path,     class: "text-white-50 text-decoration-none d-block py-1" %></li>
          <li><%= link_to "Payment Methods",     storefront_payment_methods_path,  class: "text-white-50 text-decoration-none d-block py-1" %></li>
          <li><%= link_to "FAQ",                 storefront_faq_path,              class: "text-white-50 text-decoration-none d-block py-1" %></li>
        </ul>
      </div>

      <!-- Customer Service -->
      <div class="col-6 col-md-3">
        <h6 class="text-uppercase fw-bold mb-3">Customer Service</h6>
        <div class="text-white-50 small mb-3">
          Tel: 123-456-7890<br>
          Email: helpline@prairienaturals.com
        </div>
        <div class="d-flex align-items-center gap-3">
          <a href="#" aria-label="Instagram" class="text-white-50 hover-text-white"><i class="bi bi-instagram fs-5"></i></a>
          <a href="#" aria-label="Facebook"  class="text-white-50 hover-text-white"><i class="bi bi-facebook fs-5"></i></a>
          <a href="#" aria-label="YouTube"   class="text-white-50 hover-text-white"><i class="bi bi-youtube fs-5"></i></a>
          <a href="#" aria-label="Twitter X" class="text-white-50 hover-text-white"><i class="bi bi-twitter-x fs-5"></i></a>
        </div>
      </div>

    </div>

    <hr class="border-secondary my-4">

    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center gap-3">
      <p class="text-white-50 mb-0">© <%= Time.current.year %> Prairie Naturals. All rights reserved.</p>
      <div class="d-flex gap-3 small">
        <%= link_to "Privacy Policy", storefront_privacy_policy_path, class: "text-white-50 text-decoration-none" %>
        <%= link_to "Terms of Service", storefront_terms_path, class: "text-white-50 text-decoration-none" %>
      </div>
    </div>
  </div>
</footer>


  </body>
</html>
